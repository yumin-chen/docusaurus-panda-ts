"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getOwnPropSymbols = Object.getOwnPropertySymbols;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __propIsEnum = Object.prototype.propertyIsEnumerable;
var __objRest = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/index.ts
var src_exports = {};
__export(src_exports, {
  KEY_TRAILING_BLOCK: () => KEY_TRAILING_BLOCK,
  createTrailingBlockPlugin: () => createTrailingBlockPlugin,
  withTrailingBlock: () => withTrailingBlock
});
module.exports = __toCommonJS(src_exports);

// src/createTrailingBlockPlugin.ts
var import_server2 = require("@udecode/plate-common/server");

// src/withTrailingBlock.ts
var import_server = require("@udecode/plate-common/server");
var import_slate = require("slate");
var withTrailingBlock = (editor, _a) => {
  var {
    options: _b
  } = _a, _c = _b, { level, type = (0, import_server.getPluginType)(editor, import_server.ELEMENT_DEFAULT) } = _c, query = __objRest(_c, ["level", "type"]);
  const { normalizeNode } = editor;
  editor.normalizeNode = ([currentNode, currentPath]) => {
    if (currentPath.length === 0) {
      const lastChild = (0, import_server.getLastNodeByLevel)(editor, level);
      const lastChildNode = lastChild == null ? void 0 : lastChild[0];
      if (!lastChildNode || lastChildNode.type !== type && (0, import_server.queryNode)(lastChild, query)) {
        const at = lastChild ? import_slate.Path.next(lastChild[1]) : [0];
        (0, import_server.insertElements)(editor, editor.blockFactory({ type }, at), { at });
        return;
      }
    }
    return normalizeNode([currentNode, currentPath]);
  };
  return editor;
};

// src/createTrailingBlockPlugin.ts
var KEY_TRAILING_BLOCK = "trailingBlock";
var createTrailingBlockPlugin = (0, import_server2.createPluginFactory)({
  key: KEY_TRAILING_BLOCK,
  options: {
    level: 0
  },
  then: (editor) => ({
    type: (0, import_server2.getPluginType)(editor, import_server2.ELEMENT_DEFAULT)
  }),
  withOverrides: withTrailingBlock
});
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  KEY_TRAILING_BLOCK,
  createTrailingBlockPlugin,
  withTrailingBlock
});
//# sourceMappingURL=index.js.map