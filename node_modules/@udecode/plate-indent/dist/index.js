"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getOwnPropSymbols = Object.getOwnPropertySymbols;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __propIsEnum = Object.prototype.propertyIsEnumerable;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues = (a, b) => {
  for (var prop in b || (b = {}))
    if (__hasOwnProp.call(b, prop))
      __defNormalProp(a, prop, b[prop]);
  if (__getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(b)) {
      if (__propIsEnum.call(b, prop))
        __defNormalProp(a, prop, b[prop]);
    }
  return a;
};
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/index.ts
var src_exports = {};
__export(src_exports, {
  KEY_INDENT: () => KEY_INDENT,
  KEY_TEXT_INDENT: () => KEY_TEXT_INDENT,
  createIndentPlugin: () => createIndentPlugin,
  createTextIndentPlugin: () => createTextIndentPlugin,
  indent: () => indent,
  onKeyDownIndent: () => onKeyDownIndent,
  outdent: () => outdent,
  setIndent: () => setIndent,
  useIndentButton: () => useIndentButton,
  useOutdentButton: () => useOutdentButton,
  withIndent: () => withIndent
});
module.exports = __toCommonJS(src_exports);

// src/createIndentPlugin.ts
var import_server3 = require("@udecode/plate-common/server");

// src/onKeyDownIndent.ts
var import_plate_common = require("@udecode/plate-common");

// src/transforms/setIndent.ts
var import_server = require("@udecode/plate-common/server");
var setIndent = (editor, {
  getNodesOptions,
  offset = 1,
  setNodesProps,
  unsetNodesProps = []
}) => {
  const { nodeKey } = (0, import_server.getPluginInjectProps)(editor, KEY_INDENT);
  const _nodes = (0, import_server.getNodeEntries)(editor, __spreadValues({
    block: true,
    mode: "lowest"
  }, getNodesOptions));
  const nodes = Array.from(_nodes);
  (0, import_server.withoutNormalizing)(editor, () => {
    nodes.forEach(([node, path]) => {
      var _a, _b;
      const blockIndent = (_a = node[nodeKey]) != null ? _a : 0;
      const newIndent = blockIndent + offset;
      const props = (_b = setNodesProps == null ? void 0 : setNodesProps({ indent: newIndent })) != null ? _b : {};
      if (newIndent <= 0) {
        (0, import_server.unsetNodes)(editor, [nodeKey, ...unsetNodesProps], {
          at: path
        });
      } else {
        (0, import_server.setElements)(editor, __spreadValues({ [nodeKey]: newIndent }, props), { at: path });
      }
    });
  });
};

// src/transforms/indent.ts
var indent = (editor, options) => {
  setIndent(editor, __spreadValues({ offset: 1 }, options));
};

// src/transforms/outdent.ts
var outdent = (editor, options) => {
  setIndent(editor, __spreadValues({ offset: -1 }, options));
};

// src/onKeyDownIndent.ts
var onKeyDownIndent = (editor) => (e) => {
  if (e.defaultPrevented)
    return;
  if (import_plate_common.Hotkeys.isTab(editor, e)) {
    e.preventDefault();
    indent(editor);
  }
  if (import_plate_common.Hotkeys.isUntab(editor, e)) {
    e.preventDefault();
    outdent(editor);
  }
};

// src/withIndent.ts
var import_server2 = require("@udecode/plate-common/server");
var withIndent = (editor, {
  inject: { props: { validTypes } = {} },
  options: { indentMax }
}) => {
  const { normalizeNode } = editor;
  editor.normalizeNode = ([node, path]) => {
    const element = node;
    const { type } = element;
    if (type) {
      if (validTypes.includes(type)) {
        if (indentMax && element.indent && element.indent > indentMax) {
          (0, import_server2.setElements)(editor, { indent: indentMax }, { at: path });
          return;
        }
      } else if (element.indent) {
        (0, import_server2.unsetNodes)(editor, "indent", { at: path });
        return;
      }
    }
    return normalizeNode([node, path]);
  };
  return editor;
};

// src/createIndentPlugin.ts
var KEY_INDENT = "indent";
var createIndentPlugin = (0, import_server3.createPluginFactory)({
  handlers: {
    onKeyDown: onKeyDownIndent
  },
  key: KEY_INDENT,
  options: {
    offset: 24,
    unit: "px"
  },
  then: (editor, { options: { offset, unit } = {} }) => ({
    inject: {
      props: {
        nodeKey: KEY_INDENT,
        styleKey: "marginLeft",
        transformNodeValue: ({ nodeValue }) => nodeValue * offset + unit,
        validTypes: [(0, import_server3.getPluginType)(editor, import_server3.ELEMENT_DEFAULT)]
      }
    }
  }),
  withOverrides: withIndent
});

// src/createTextIndentPlugin.ts
var import_server4 = require("@udecode/plate-common/server");
var KEY_TEXT_INDENT = "textIndent";
var createTextIndentPlugin = (0, import_server4.createPluginFactory)({
  key: KEY_TEXT_INDENT,
  options: {
    offset: 24,
    unit: "px"
  },
  then: (editor, { options: { offset, unit } = {} }) => ({
    inject: {
      props: {
        nodeKey: KEY_TEXT_INDENT,
        styleKey: "textIndent",
        transformNodeValue({ nodeValue }) {
          return nodeValue * offset + unit;
        },
        validTypes: [(0, import_server4.getPluginType)(editor, import_server4.ELEMENT_DEFAULT)]
      }
    }
  })
});

// src/hooks/useIndentButton.ts
var import_plate_common2 = require("@udecode/plate-common");
var useIndentButton = () => {
  const editor = (0, import_plate_common2.useEditorRef)();
  return {
    props: {
      onClick: () => {
        indent(editor);
      },
      onMouseDown: (e) => {
        e.preventDefault();
      }
    }
  };
};

// src/hooks/useOutdentButton.ts
var import_plate_common3 = require("@udecode/plate-common");
var useOutdentButton = () => {
  const editor = (0, import_plate_common3.useEditorRef)();
  return {
    props: {
      onClick: () => {
        outdent(editor);
      },
      onMouseDown: (e) => {
        e.preventDefault();
      }
    }
  };
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  KEY_INDENT,
  KEY_TEXT_INDENT,
  createIndentPlugin,
  createTextIndentPlugin,
  indent,
  onKeyDownIndent,
  outdent,
  setIndent,
  useIndentButton,
  useOutdentButton,
  withIndent
});
//# sourceMappingURL=index.js.map